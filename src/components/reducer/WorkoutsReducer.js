const WorkoutsReducer = ( state, action ) => {
    switch(action.type){
        case 'ADD_WORKOUT':
            return
        case 'EDIT_WORKOUT':
            return
        case 'REMOVE_WORKOUT':
            return
        default:
            return
    }
}

export default WorkoutsReducer;